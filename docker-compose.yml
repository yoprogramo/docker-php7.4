version: "3"

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    image: biblioeteca/php7.4:${VERSION}

  php-xdebug:
    build:
      context: .
      dockerfile: Dockerfile-xdebug
    image: biblioeteca/php7.4:${VERSION}-xdebug

  
